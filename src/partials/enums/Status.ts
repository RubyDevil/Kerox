export enum Status {
   Unknown = 'Unknown',
   Idle = 'Idle',
   Busy = 'Busy',
   Dead = 'Dead',
}

export enum Info {
   Unknown = 'Unknown',
   Initializing = 'Initializing',
   ValidatingProxies = 'Validating Proxies',
   Stressing = 'Currently Converging DDoS Attack'
}
